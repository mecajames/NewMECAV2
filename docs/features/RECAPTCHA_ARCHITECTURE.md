# Google reCAPTCHA v3 - Architecture Diagram

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER BROWSER                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌───────────────────────────────────────────────────────┐     │
│  │  Page (HostEventPage.tsx / ContactPage.tsx)          │     │
│  │                                                       │     │
│  │  const { executeRecaptcha } = useRecaptcha('action') │     │
│  │                                                       │     │
│  │  handleSubmit() {                                     │     │
│  │    1. executeRecaptcha() ──────────┐                │     │
│  │    2. if valid, submit form         │                │     │
│  │  }                                  │                │     │
│  └─────────────────────────────────────┼────────────────┘     │
│                                        │                       │
│  ┌─────────────────────────────────────▼────────────────┐     │
│  │  useRecaptcha Hook (apiHooks.ts)                     │     │
│  │                                                       │     │
│  │  1. window.grecaptcha.execute(siteKey, { action })   │     │
│  │     ─────────────────────────┐                       │     │
│  │                              │                       │     │
│  │  3. await recaptchaApi.verify(token, action) ───┐   │     │
│  │                                                  │   │     │
│  │  4. return success                               │   │     │
│  └──────────────────────────────────────────────────┼───┘     │
│                                                     │          │
│  ┌──────────────────────────────────────────────────▼───┐     │
│  │  ReCaptchaProvider (ReCaptchaProvider.tsx)          │     │
│  │                                                      │     │
│  │  - Loads Google reCAPTCHA script on mount           │     │
│  │  - <script src="https://www.google.com/             │     │
│  │            recaptcha/api.js?render=${siteKey}">     │     │
│  └──────────────────────────────────────────────────────┘     │
└─────────────────────────────────────────────────────────────────┘
                                  │
                                  │ 2. Get token from Google
                                  ▼
┌─────────────────────────────────────────────────────────────────┐
│                      GOOGLE reCAPTCHA API                       │
│                  (https://www.google.com)                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  - Analyzes user behavior                                       │
│  - Returns token (JWT)                                          │
│  - Token contains: score, action, timestamp, etc.               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                                  │
                                  │ 3. Send token to backend
                                  ▼
┌─────────────────────────────────────────────────────────────────┐
│                      FRONTEND API CLIENT                        │
│               (recaptcha.api-client.ts)                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  recaptchaApi.verify(token, action)                             │
│                                                                 │
│  POST http://localhost:3001/api/recaptcha/verify                │
│  Body: { token, action }                                        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                                  │
                                  │ HTTP POST
                                  ▼
┌─────────────────────────────────────────────────────────────────┐
│                       BACKEND (NestJS)                          │
│                  http://localhost:3001                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌───────────────────────────────────────────────────────┐     │
│  │  RecaptchaController (@Controller('api/recaptcha'))  │     │
│  │                                                       │     │
│  │  @Post('verify')                                      │     │
│  │  async verify(@Body() dto) {                          │     │
│  │    return recaptchaService.verifyToken(               │     │
│  │      dto.token,                                       │     │
│  │      dto.action                                       │     │
│  │    );                                                 │     │
│  │  }                                                    │     │
│  └───────────────────────────┬───────────────────────────┘     │
│                              │                                 │
│                              ▼                                 │
│  ┌───────────────────────────────────────────────────────┐     │
│  │  RecaptchaService (@Injectable)                      │     │
│  │                                                       │     │
│  │  async verifyToken(token, action) {                   │     │
│  │    1. POST to Google API ──────────┐                │     │
│  │                                     │                │     │
│  │    3. if (!success) throw error     │                │     │
│  │    4. if (score < 0.5) throw error  │                │     │
│  │    5. if (action mismatch) throw    │                │     │
│  │    6. return true                   │                │     │
│  │  }                                  │                │     │
│  └─────────────────────────────────────┼────────────────┘     │
│                                        │                       │
└────────────────────────────────────────┼───────────────────────┘
                                         │
                                         │ 2. Verify with Google
                                         ▼
┌─────────────────────────────────────────────────────────────────┐
│              GOOGLE reCAPTCHA VERIFICATION API                  │
│         https://www.google.com/recaptcha/api/siteverify         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  POST with:                                                     │
│    - secret: RECAPTCHA_SECRET_KEY                               │
│    - response: token                                            │
│                                                                 │
│  Returns:                                                       │
│  {                                                              │
│    success: true/false,                                         │
│    score: 0.0 - 1.0,                                            │
│    action: "host_event",                                        │
│    challenge_ts: "2025-01-13T10:30:00Z",                        │
│    hostname: "localhost"                                        │
│  }                                                              │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## 📊 File Organization

```
NewMECAV2/
│
├── apps/
│   │
│   ├── backend/                    # Backend (NestJS)
│   │   ├── src/
│   │   │   ├── recaptcha/
│   │   │   │   ├── recaptcha.service.ts      ✅ Verification logic
│   │   │   │   ├── recaptcha.controller.ts   ✅ API endpoint
│   │   │   │   └── recaptcha.module.ts       ✅ NestJS module
│   │   │   └── app.module.ts                 ✅ Imports RecaptchaModule
│   │   └── .env.example                      ✅ RECAPTCHA_SECRET_KEY
│   │
│   └── frontend/                   # Frontend (React + Vite)
│       ├── src/
│       │   ├── api-client/
│       │   │   └── recaptcha.api-client.ts   ✅ API client
│       │   ├── shared/
│       │   │   └── recaptcha/
│       │   │       ├── apiHooks.ts           ✅ useRecaptcha hook
│       │   │       ├── ReCaptchaProvider.tsx ✅ Script loader
│       │   │       └── index.ts              ✅ Barrel export
│       │   ├── pages/
│       │   │   ├── HostEventPage.tsx         ✅ Uses reCAPTCHA
│       │   │   └── ContactPage.tsx           ✅ Uses reCAPTCHA
│       │   └── App.tsx                       ✅ Wraps with provider
│       └── .env.example                      ✅ VITE_RECAPTCHA_SITE_KEY
│
└── Documentation/
    ├── RECAPTCHA_SETUP.md                    ✅ Setup guide
    ├── RECAPTCHA_IMPLEMENTATION_SUMMARY.md   ✅ Summary
    └── RECAPTCHA_CHECKLIST.md                ✅ Checklist
```

## 🔄 Request Flow (Sequence)

```
User                Frontend              Google              Backend             Google API
 │                     │                    │                   │                    │
 │ 1. Submit form      │                    │                   │                    │
 ├────────────────────>│                    │                   │                    │
 │                     │                    │                   │                    │
 │                     │ 2. Execute         │                   │                    │
 │                     ├───────────────────>│                   │                    │
 │                     │    reCAPTCHA       │                   │                    │
 │                     │                    │                   │                    │
 │                     │ 3. Return token    │                   │                    │
 │                     │<───────────────────┤                   │                    │
 │                     │                    │                   │                    │
 │                     │ 4. Verify token    │                   │                    │
 │                     ├───────────────────────────────────────>│                    │
 │                     │    POST /api/recaptcha/verify          │                    │
 │                     │                    │                   │                    │
 │                     │                    │                   │ 5. Verify with     │
 │                     │                    │                   │    Google          │
 │                     │                    │                   ├───────────────────>│
 │                     │                    │                   │ POST /siteverify   │
 │                     │                    │                   │                    │
 │                     │                    │                   │ 6. Verification    │
 │                     │                    │                   │    result          │
 │                     │                    │                   │<───────────────────┤
 │                     │                    │                   │ { success, score } │
 │                     │                    │                   │                    │
 │                     │ 7. Success/Fail    │                   │                    │
 │                     │<───────────────────────────────────────┤                    │
 │                     │                    │                   │                    │
 │ 8. Form submitted   │                    │                   │                    │
 │    or error shown   │                    │                   │                    │
 │<────────────────────┤                    │                   │                    │
 │                     │                    │                   │                    │
```

## 🎯 Key Components

### Frontend Components

| Component | Purpose | Location |
|-----------|---------|----------|
| **ReCaptchaProvider** | Loads Google script | `shared/recaptcha/ReCaptchaProvider.tsx` |
| **useRecaptcha** | React hook for execution | `shared/recaptcha/apiHooks.ts` |
| **recaptchaApi** | API client | `api-client/recaptcha.api-client.ts` |
| **HostEventPage** | Protected form | `pages/HostEventPage.tsx` |
| **ContactPage** | Protected form | `pages/ContactPage.tsx` |

### Backend Components

| Component | Purpose | Location |
|-----------|---------|----------|
| **RecaptchaService** | Verification logic | `recaptcha/recaptcha.service.ts` |
| **RecaptchaController** | API endpoint | `recaptcha/recaptcha.controller.ts` |
| **RecaptchaModule** | NestJS module | `recaptcha/recaptcha.module.ts` |

## 🔐 Security Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                      SECURITY LAYERS                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Layer 1: Frontend Execution                                    │
│  ┌──────────────────────────────────────────────────────┐      │
│  │  - Google analyzes user behavior                     │      │
│  │  - Generates score (0.0 - 1.0)                       │      │
│  │  - Returns signed JWT token                          │      │
│  │  - Token cannot be forged                            │      │
│  └──────────────────────────────────────────────────────┘      │
│                                                                 │
│  Layer 2: Backend Verification                                  │
│  ┌──────────────────────────────────────────────────────┐      │
│  │  - Validates token with Google API                   │      │
│  │  - Checks score >= 0.5                               │      │
│  │  - Validates action matches                          │      │
│  │  - Cannot be bypassed from client                    │      │
│  └──────────────────────────────────────────────────────┘      │
│                                                                 │
│  Layer 3: Environment Protection                                │
│  ┌──────────────────────────────────────────────────────┐      │
│  │  - Secret key in .env (not in code)                  │      │
│  │  - .env in .gitignore                                │      │
│  │  - Different keys for dev/prod                       │      │
│  │  - HTTPS required in production                      │      │
│  └──────────────────────────────────────────────────────┘      │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## 📈 Score Interpretation

```
Score Range │ Interpretation │ Action
────────────┼────────────────┼─────────────────────────
0.9 - 1.0   │ Very likely    │ ✅ Allow
            │ legitimate     │
────────────┼────────────────┼─────────────────────────
0.5 - 0.9   │ Likely human   │ ✅ Allow (with caution)
────────────┼────────────────┼─────────────────────────
0.0 - 0.5   │ Likely bot     │ ❌ Reject
────────────┼────────────────┼─────────────────────────

Note: Threshold set to 0.5 (configurable in recaptcha.service.ts)
```

## 🎬 Action Names

| Form | Action Name | Purpose |
|------|-------------|---------|
| Host Event | `host_event` | Identify event hosting submissions |
| Contact Us | `contact_us` | Identify contact form submissions |
| (Future) | `registration` | For user registration |
| (Future) | `login` | For login attempts |

---

**Architecture follows**: ONBOARDING.md patterns
**Framework**: NestJS (Backend) + React (Frontend)
**reCAPTCHA Version**: v3 (Invisible)
