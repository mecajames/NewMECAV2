# ============================================
# NewMECA V2 - Staging Frontend Build Args
# ============================================
# Copy this file to deploy/.env.staging and fill in real values.
# NEVER commit .env.staging with real secrets!
#
# These VITE_* values are used by deploy.ps1 and baked into the
# frontend at Docker build time via --build-arg.
#
# Backend secrets are in deploy/.env.backend (see .env.backend.example)
# ============================================

VITE_SUPABASE_URL=https://db.mecacaraudio.com
VITE_SUPABASE_ANON_KEY=your-anon-key-here
VITE_API_URL=
VITE_RECAPTCHA_SITE_KEY=your-site-key-here
VITE_STRIPE_PUBLISHABLE_KEY=
VITE_GA4_MEASUREMENT_ID=G-XXXXXXXXXX
