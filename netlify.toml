[build]
  command = "npm run build:frontend"
  publish = "apps/frontend/dist"

# Environment variables are set in Netlify UI:
# VITE_SUPABASE_URL
# VITE_SUPABASE_ANON_KEY

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Redirect API calls to backend (uncomment and update URL when backend is deployed)
# [[redirects]]
#   from = "/api/*"
#   to = "https://your-backend-url.com/api/:splat"
#   status = 200
#   force = true
